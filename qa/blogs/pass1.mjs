import {chromium} from 'playwright';import {mkdir,writeFile} from 'node:fs/promises';
await mkdir('qa/blogs/pass1',{recursive:true});const browser=await chromium.launch({channel:'chrome'});const errors=[],results=[];
for(const width of [1440,768,390]){const page=await browser.newPage({viewport:{width,height:width===1440?900:width===768?1024:844}});page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});
for(const [name,path] of [['listing','/blogs'],['article','/blogs/understanding-fixed-deposits'],['login','/login']]){await page.goto(`http://127.0.0.1:5173${path}`,{waitUntil:'networkidle'});await page.waitForTimeout(1000);await page.screenshot({path:`qa/blogs/pass1/${width}-${name}.png`});results.push({width,name,overflow:await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth)});}
await page.locator('#admin-username').fill('krish');await page.locator('#admin-password').fill('1153');await page.getByRole('button',{name:'Login',exact:true}).click();await page.waitForTimeout(450);await page.screenshot({path:`qa/blogs/pass1/${width}-dashboard.png`});await page.getByRole('button',{name:'Create blog',exact:false}).click();await page.waitForTimeout(450);await page.screenshot({path:`qa/blogs/pass1/${width}-editor.png`});await page.close();}
await writeFile('qa/blogs/pass1/results.json',JSON.stringify({results,errors},null,2));console.log(JSON.stringify({results,errors}));await browser.close();
